# AGENTS.md — [项目名]

> **通用 agent 入职总入口。**任何 AI 编码代理冷启动打开本仓库，先完整读完本文件再动手。

## 快速开始

1. 跑"冷启动自检"（下节），全绿才允许继续。
2. 读 `docs/architecture.md` 中与本次任务相关的章节——它是系统设计唯一权威。
3. 读任务涉及域的历史经验：`docs/risks/` 下对应文件。
4. 明确任务边界后动手；全程遵守"铁律"。

## 冷启动自检（接到改代码任务时动手前必跑）

接到改代码任务时，先逐条跑下面的检查；任一条不通过就停下，向人报告环境状态，不许带病开工：

```bash
git rev-parse --abbrev-ref HEAD                  # 按铁律 8：应在新分支上，不得在受保护分支直接开工
[环境就绪检查命令]                                 # 如 test -f .env 等本机环境标记
[架构红线守护命令]                                 # 项目专用的守护测试，要求秒级可跑
```

纯问答、文档阅读、调研类任务可跳过。

## 常用命令

> 每条注明验收含义（如"必须全绿才能交付"）。

```bash
[测试命令]
[类型检查/静态检查命令]
[本地启动命令]
[构建命令]
[项目特有的数据/任务命令]
```

## 铁律（违反即返工）

1. **[项目架构红线]**。本项目最重要的一条技术禁令（如"不引入某类外部服务"、"某层不许直连某存储"）。
   *验收*：对应守护测试随 `[测试命令]` 全绿。
2. **命名与契约术语唯一权威为 `docs/architecture.md`**。模块/函数/变量名、前后端类型与字段、表名列名、接口字段，均直接复用文档中对应术语的字面形式；不得自创同义改写；驼峰/下划线互转一律反对。
   *验收*：新增/改名标识符能在 architecture.md 或其引用注册表（常数定义、类型定义）中找到对应原文；相关守护检查全绿。
3. **业务口径只有一个来源**。展示层不得自行凑数、推断业务口径；口径只从其权威层取（如"指标口径只从后端来"）。
   *验收*：展示层不存在自行推断业务口径的逻辑。
4. **行为改动先写失败测试，再实现到绿**。纯视觉/样式/文案修补例外；修 bug 必须带复现测试。
   *验收*：`git log` 能看到测试先于实现（红→绿）；测试命令全绿。
5. **先跑通验证再说完成**。禁止用"应该能过"式声明代替真实验证。
   *验收*：测试、类型检查、构建按"常用命令"节注明的验收全部实际跑通。
6. **`docs/architecture.md` 只读**。设计变化找产品/设计对齐，不在代码里私改语义。
   *验收*：仓库历史里不存在未走公开评审流程的 architecture.md 修改。
7. **push 前按 checklist 完成检查**，完整核对清单见 `docs/before-push-checklist.md`。任何检查项勾不出来时必须在 PR 里显式报告未完成项与原因，不许静默跳过。
   *验收*：PR 内容包括逐项检查结果；勾不出来的项已写明原因并显式报给人。
8. **提交代码走新分支 + PR（默认合入 `[主开发分支]`）**，一个分支/PR 只做一件事。
   - 分支命名：`<type>/<语义短名>`；type 与 commit message 的 type 同集合（feature|fix|docs|refactor|chore|test|perf），短名全小写英文加连字符（如 `feature/user-export`），不带人名、日期；工具自动生成的隔离分支（如 `synergy/*`）不受此限。
   - commit 纪律：对话中途可随手 commit 保存进度，但零散 commit 不逐个 push；push 前 squash 成按"完整一件事"组织的历史，一次性推送。
   - PR 纪律：代理把改动推进到就绪状态后停下；建 PR 由人触发，不自行发起。
   *验收*：PR 创建动作来自人的明确指令；分支相对目标分支的历史呈现为完整一件事；当前分支是新分支且 PR 目标为 `[主开发分支]`。

## 人类权威边界

遇到以下决策点时，向人陈述选项与依据、由人拍板，不单方面决定：

- 涉及业务语义、干系人优先级、架构取舍的判断（与铁律 6 同向：设计语义只从人来）。
- 以人的身份、名义或口吻对外动作：发消息、回复工单、审批、对外表态。准备草稿可以，发送动作必须经人确认。
- 需求模糊且分支互斥、猜错即返工时；能靠读代码/文档消除的歧义先自行消除，剩下的才问人。

## 关键分层（改代码前先对号入座）

> 按项目实际结构填写：每层一行，写清职责边界；多层结构（如前后端）分多张表。

| 层 | 目录 | 能碰什么 |
|---|---|---|
| [层名] | `[路径]` | [职责边界] |

## 历史经验（分域登记，按需去读）

- 各领域踩坑登记在 `docs/risks/` 下，一域一文件，新条目加在文件最上方；格式约定见 `docs/risks/README.md`。
- 防线已稳的退役条目移入 `docs/archived/`，不再进 context。
- 代码规范与反面案例：`docs/code-standards.md`。

接到某域的开发任务时，根据任务复杂度先读该域的 risks 文件再动手。

## 人机协作度量

- 协作过程产生的坑与返工事件的登记口径：`docs/agent-collab-metrics.md`。
- 踩到新坑时顺手按该文件的记录格式补一条；门槛与分类口径都在文件里，不在本节展开。

## 文档

- `docs/architecture.md`：系统设计唯一权威。
- `docs/before-push-checklist.md`：push 前三态检查清单。
- `docs/code-standards.md`：工程规范与反面案例（DRY + 文档规范）。
- `docs/agent-collab-metrics.md`：人机协作度量登记口径。
- `docs/decisions/README.md`：决策记录的格式与存放约定（一个 PR 一篇）。
- `docs/risks/README.md`：分域踩坑登记的格式约定。
- `docs/archived/README.md`：退役内容归档规则。
