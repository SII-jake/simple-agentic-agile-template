# INIT.md — 模板初始化引导

> **本文件仅在仓库尚未初始化为具体项目时存在。**初始化完成后必须删除本文件（含 git 历史里的最终形态之外不应再有副本）。
> 任何 agent 冷启动打开本仓库：若发现 `INIT.md` 存在，先执行本文档，不得修改任何仓库文件；若不存在，直接读 `AGENTS.md`。

---

## 何时执行

仓库根目录存在 `INIT.md` 即视为待初始化。初始化完成后立即删除本文件。

## 目的

收集初始化所需信息，并以两个原子 commit 落地：

- **commit 1**：原样提交模板基线（制造 diff 锚点）。
- **commit 2**：应用项目定制（替换 `AGENTS.md`、`README.md`、相关 docs 内容），并删除本文件。

## 收集项（一次只问一个问题，等到答案再问下一个）

1. **项目名**（必填）——替换 `AGENTS.md` 与 `README.md` 标题处的 `[项目名]` 占位符。
2. **Git 分支模型**（必填）——主开发分支名（PR 默认目标）与受保护分支名，写入 `AGENTS.md` 铁律 8 与"冷启动自检"检查。
3. **冷启动自检的项目特有检查**（必填）——环境就绪检查命令、架构红线守护命令，填入 `AGENTS.md` "冷启动自检"代码块。
4. **常用命令块**（必填）——测试/类型检查/启动/构建命令及各条验收含义，填入 `AGENTS.md` "常用命令"代码块。
5. **关键分层表**（可延后）——目录与职责边界，填入 `AGENTS.md` 表格；延后则登记 TBD 并在收尾清单中显式报出。
6. **代码托管/PR 平台与 issue 跟踪方式**（可延后）——记入 `AGENTS.md` 或项目工作流；延后则记 TBD。

## 落地

汇总收集结果，列给人确认。经人确认后：

1. `git add -A`（**包括本 INIT.md**）并 commit 为 **模板基线**。
2. 应用项目定制（替换占位符、填写命令、写入铁律 1 项目红线、填关键分层等）。
3. **删除本文件 `INIT.md`**（同时清理 git 暂存区里的副本）。
4. commit 为 **项目定制**。
5. 跑"收尾验证清单"，未完成项显式报给人。

## AGENTS.md 成品形态

初始化完成后，`AGENTS.md` 中不再保留任何初始化相关内容，包括但不限于：

- 面向"待初始化"状态的入口判断（如"看标题占位符走初始化"等表述）
- "初始化时替换"“初始化时填写”类占位提示（成品必须已填好真实值）
- "引导对话"“收集项”“收尾验证”等流程性内容

`AGENTS.md` 应为该项目的**纯入职手册**：只含"快速开始 / 冷启动自检 / 常用命令 / 铁律 / 人类权威边界 / 关键分层 / 历史经验 / 协作度量 / 文档"。

## 收尾验证清单（未完成项必须显式报给人，不许静默跳过）

- [ ] `AGENTS.md` 标题处的占位符替换为真实项目名，全文不含"待初始化""引导对话"等流程性内容。
- [ ] 铁律 1 写入项目真实红线；自检三条命令全部实测通过。
- [ ] 常用命令块全部实测跑通。
- [ ] 关键分层表已填写，或显式登记 TBD。
- [ ] `INIT.md` 已从仓库删除（最终形态里不再存在）。
- [ ] git 历史中存在基线 commit 与定制 commit 两个提交。
- [ ] 人知道仍需手工补齐的遗留项（如有）。

## 非对话入口（不想逐条对话时，可整段发给 agent）

```
按 INIT.md 将本仓库初始化为我的项目。信息如下：
- 项目名：<...>
- 主开发分支 / 受保护分支：<...> / <...>
- 环境就绪检查：<...>；红线守护命令：<...>
- 测试/类型检查/启动/构建命令：<...>
按两个 commit 落地（基线 + 定制，删除 INIT.md），随后跑收尾验证清单并报告未完成项。
```
