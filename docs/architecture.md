# [项目名] 系统设计

> 本文档是系统设计唯一权威：命名与契约术语（AGENTS.md 铁律 2）、业务口径（铁律 3）、分层边界都以本文档为唯一事实源。
> 修改纪律：本文档对 agent 只读；设计变化经公开评审后更新（铁律 6）。书写规范遵循 `docs/code-standards.md` 第二节：一完整句一行、无营销词、术语照抄。

## 一、术语表

> 全仓命名唯一事实源。模块名、函数名、常量名、类型字段、表名列名，必须能在本节或下方引用注册表中找到对应原文。
> 新增术语先登记后使用；同一概念只保留一个词，不留同义词。

| 术语 | 中文含义 | 定义与口径 | 所属域 |
|---|---|---|---|
| [TermOne] | [中文名] | [一句话定义；涉及业务口径时只写权威计算方式] | [域] |

- 引用注册表（术语的代码落地处）：常量定义 `[路径]`；类型定义 `[路径]`。

## 二、产品架构

> 系统是什么、给谁用、主链路是什么。本节只写产品语义，不写实现。

- 定位：[一两句话说清系统定位]。
- 用户与场景：[主要用户角色，及各自的核心场景]。
- 功能域划分：[域1]、[域2]……（与 `docs/risks/` 的分域对齐）。
- 主链路：[从输入到产出的端到端路径，一段话]。

## 三、逻辑架构

> 系统的逻辑分层与职责边界，不关心代码目录与部署位置。

- 分层与职责：每层一行——层名、职责、不做什么。
  - [层名]：[职责]；不[越界行为]。
- 层间依赖方向：[如"展示层只依赖服务层输出，不得直连存储层"]。
- 架构红线：[项目最重要的一条技术禁令；与 AGENTS.md 铁律 1 保持同文]。

## 四、数据模型

> 核心实体、字段口径与数据归属。每份数据只有一个写入方（`docs/code-standards.md` DRY 第 4 条）。

- 实体关系：[核心实体及其关键关系，文字或 ER 示意]。
- 关键表/集合：

| 表名 | 用途 | 写入方（唯一） | 主要消费方 |
|---|---|---|---|
| [table_name] | [一句话用途] | [唯一写入方] | [消费方] |

- 口径权威层：[关键指标/业务口径从哪一层产出；与 AGENTS.md 铁律 3 保持同文]。

## 五、代码架构

> 逻辑层到代码目录的映射。与 AGENTS.md "关键分层"表保持一致；两处不一致时以本文档为准并修正 AGENTS.md。

| 逻辑层 | 目录 | 职责边界 |
|---|---|---|
| [层名] | `[路径]` | [能碰什么、不能碰什么] |

- 命名约定：[文件/模块、类型、常量、事件的命名规则]。
- 引用注册表位置：常量定义 `[路径]`；类型定义 `[路径]`（与术语表引用注册表同处）。

## 六、部署架构

> 系统运行形态：进程、依赖与环境。改部署形态先改本节。

- 运行拓扑：[服务/进程清单与调用关系，文字或示意]。
- 外部依赖：[数据库、缓存、队列、第三方服务，每条注明用途]。
- 环境划分：[开发/预发/生产的差异与发布路径]。
