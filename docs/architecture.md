# [项目名] 系统设计

> 系统设计唯一权威：术语、口径、分层均以本文档为唯一事实源。按项目实际填写；用不到的节整节删除。

## 一、术语表

> 命名唯一事实源：先登记后使用，同一概念只保留一个词。涉及业务口径的术语在"定义与口径"列写权威计算方式。

| 术语 | 中文含义 | 定义与口径 | 所属域 |
|---|---|---|---|
| [TermOne] | [中文名] | [一句话定义] | [域] |

代码侧注册表（术语的落地处）：常量定义 `[路径]`；类型定义 `[路径]`。

## 二、产品架构

> 系统是什么、给谁用、主链路是什么；只写产品语义，不写实现。

- 定位与目标用户：[一两句话]。
- 功能域划分：[域1]、[域2]……（与 `docs/risks/` 的分域对齐）。
- 主链路：[从输入到产出的端到端路径，一段话]。

## 三、逻辑架构

> 逻辑分层与职责边界，不涉及代码目录与部署位置。

- 分层与职责（每层一行，写清不做什么）：
  - [层名]：[职责]；不[越界行为]。
- 依赖方向：[如"展示层只依赖服务层输出，不直连存储层"]。

## 四、数据模型

> 核心实体与数据归属，每份数据只有一个写入方。

- 实体关系：[核心实体及关键关系，文字或 ER 示意]。

| 表名 | 用途 | 写入方 | 主要消费方 |
|---|---|---|---|
| [table_name] | [一句话用途] | [唯一写入方] | [消费方] |

## 五、代码架构

> 逻辑层到代码目录的映射。本节为准，AGENTS.md "关键分层"是其工作副本。

| 逻辑层 | 目录 | 职责边界 |
|---|---|---|
| [层名] | `[路径]` | [能碰什么] |

- 命名约定：[文件/模块、类型、常量、事件的命名规则]。

## 六、部署架构

> 系统运行形态：进程、外部依赖与环境。

- 运行拓扑：[服务/进程清单与调用关系]。
- 外部依赖：[数据库、缓存、第三方服务，每条注明用途]。
- 环境划分：[开发/预发/生产的差异与发布路径]。
